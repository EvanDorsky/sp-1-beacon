# DEV-ONLY overlay config: build the module flasher ARMED (performs the DS
# write). Use with fw.sh dlarm. Read bluetooth/reflashing-the-module.md first;
# the write is DS-only + SS-preserving, never chip-erase.
CONFIG_SP1_BT_DOWNLOAD=y
CONFIG_SP1_BT_DOWNLOAD_ARM=y
