# DEV-ONLY overlay config: build the CYW20706 module flasher (dry-run).
# Use with fw.sh dl, which also pulls in download.overlay.
#   ./scripts/fw.sh dl        # dry-run: identity gate + DS plan, NO write
#   ./scripts/fw.sh dlarm     # ARMED: performs the DS-only write
# Requires the generated src/cybt_blobs.h (scripts/gen_blobs.py) first.
CONFIG_SP1_BT_DOWNLOAD=y
