# RELEASE overlay config: the publishable beacon firmware with on-device radio
# provisioning compiled in (fw.sh release). Requires the generated
# src/cybt_blobs.h to carry the SS identity template:
#   scripts/gen_blobs.py --minidriver ... --ds ... --ss-template <flash_dump.bin>
CONFIG_SP1_PROVISION=y
